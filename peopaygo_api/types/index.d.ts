
declare namespace NodeJS {
  PORT: number;
  // API_KEY: string;
  DB_HOST: string;
  DB_PORT: number;
  DB_USERNAME: string;
  DB_PASSWORD: string;
  DB_NAME: string;
  NODE_ENV: string;
  HASH_SALT: number;
  JWT_SECRET: string;
}